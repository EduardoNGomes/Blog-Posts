import { styled } from '@stitches/react'

export const Header = styled('header', {
  nav: {
    display: 'flex',
    alignItems: 'center',
  },

  ul: {
    display: 'flex',
    span: {
      'background-color': 'black',
    },
  },
})
